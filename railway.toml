[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py db-init && gunicorn dashboard.app:app -w 2 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:$PORT --timeout 120"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
